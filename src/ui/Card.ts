import styled from '@emotion/styled';

const Card = styled.div({
  display: 'flex',
  padding: '0.5rem',
  borderRadius: '0.375rem',
  overflow: 'hidden',
  background: '#ffffff',
  boxShadow: '0 0 10px #ccc',
});

export default Card;
